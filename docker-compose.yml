version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    # Load environment variables from .env file
    env_file:
      - .env
    restart: always
